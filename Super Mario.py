import pygame as py
import time
import random as rd
py.init()

display_width = 1080
display_height = 800
gameDisplay = py.display.set_mode((display_width,display_height)) ## resolution of game

# colours are measured in RGB --> RED, GREEN, BLUE
black = (0,0,0) # lack of colours
white = (255,255,255) # all of the colours

py.quit()
